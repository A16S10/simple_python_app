pipeline {
    agent any

    stages {
        stage('Test Credentials') {
            steps {
                script {
                    echo "Testing credentials: ${env.DOCKER_CREDS}"
                }
            }
        }
    }
}
